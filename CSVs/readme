The orginal JSON files returned from API requests are too big to upload into the GitHub. 
As well as some CSV files, the zip files are also too large to upload (>25MB).
Here I just uploaded the small-size organized CSV files organized from the JSON returns.
If you need to see all the original data please contact.
